#include <iostream>
#include<vector>

using namespace std;

int main()
{
    int n;
    cin>>n;
    
    while(n--)
    {
        int no_case,points;
        cin>>no_case;
        vector<int> arr(no_case);
        
        for (int i = 0; i < no_case; ++i) {
			cin >> arr[i];
		}
		
		
		
		
        
    }
    return 0;
}

